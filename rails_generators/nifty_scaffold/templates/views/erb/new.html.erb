<h1> New <%= singular_name.titleize %> </h1>

<%% form_for @<%= singular_name %> do |f| %>
  <%= render_form %>
  <p><%%= f.submit, 'Create', :class => 'button' %></p>
<%% end %>

<%- if action? :index -%>
<p><%%= link_to "Back to List", <%= plural_name %>_path, :class => 'button' %></p>
